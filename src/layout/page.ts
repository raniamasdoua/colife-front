/**
 * Conteneur principal des pages (même largeur max et mêmes gouttières que l’accueil).
 * À utiliser pour tout le contenu scrollable sous la barre du haut.
 */
export const PAGE_CONTAINER_CLASS = "max-w-4xl mx-auto w-full px-4";
